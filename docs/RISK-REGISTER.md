# Dex Lens risk register

Last updated: 2026-09-08

This is the lightweight register for risks that must stay visible until the
full R7 handoff pack exists. Every unresolved entry needs a named owner before
pilot sign-off.

| ID | Risk | Evidence | Impact | Current mitigation | Owner | Status |
| --- | --- | --- | --- | --- | --- | --- |
| RISK-MAC-SOCKET-CREATION | macOS Seatbelt blocks outbound connection, not socket creation, on GitHub macos-14 runners and on local Apple Silicon Macs. | CI runs 31244515811, 31244792823, and 31244920768 showed AF_INET, AF_INET6, and AF_UNIX sockets were created under the shipped profile despite `(deny system-socket)`, while the runtime proof returned `connect-denied`, `write-open-denied`, and `exec-denied`. Reproduced locally on macOS 26 / Apple Silicon on 2026-08-21. | The macOS containment story is asymmetric with Linux: no egress is enforced, but "no socket fd can exist" is not proven on the pilot platform. | **Accepted asymmetry (2026-08-21).** `MacOSStrategy.availability()` requires a fresh runtime tuple proving no-write, no-exec, and no-network, where the no-network proof is satisfied by either `socket-denied` or `connect-denied`. Egress needs `connect`/`sendto`, and `prove_containment` aborts the collection if a connection succeeds or fails ambiguously, so the guarantee the product makes to a person — nothing leaves this machine — is proven either way. The honest label is carried through to `ContainmentOutcome.proofs` and shown verbatim, never relabelled as `socket-denied`. Keep `(deny system-socket)` plus `(deny network*)` and retain the direct egress probe. | Dave | Closed — accepted |
| RISK-BOUNDED-CAPTURE-ABSENCE | A bounded capture reported `absent` for artifacts it had simply never reached, and the presence probes reported `healthy` while describing an arbitrary fraction of the approved scope. | First real-machine run, 2026-08-21: a 170k-file vault admitted 151k files, the 2048-file bound was spent on the walk order, and 149,020 admitted files were never read. The probes reported healthy on 1.4% of scope. Only `collection-exclusions` disclosed the truncation. | The product's central promise is that an assumption never masquerades as proof. Claiming absence from an unread remainder reports a capability the person **has** as one they lack, and it does so with the highest confidence label the product offers. | Capture is ordered so files named in `CLAUDE_CODE_DIAGNOSTIC_BASENAMES` are read before the bound bites (order only — the approved scope is unchanged); the file-count bound is raised to 16384 so `max_total_bytes` is again what binds; and a presence probe on an incomplete capture reports `could-not-check` with `blocked` evidence instead of `absent`, so the finding surfaces as Unknown. Partial results carry the caveat in `detail`. | Dave | Closed — fixed |
| RISK-MAC-INTERPRETER-LITERAL | The Seatbelt exec allowlist named an unresolved interpreter path, so the contained child was denied on any Mac whose Python is symlinked. | Homebrew's `sys.base_prefix` is `/opt/homebrew/opt/python@3.X/...`, a symlink into `/opt/homebrew/Cellar/...`. Seatbelt canonicalises the exec target before matching a `literal`, so the child died in `posix_spawn` and was reported as "containment could not be established". Found on 2026-08-21 during the first real-machine run against a live vault. | Every Homebrew-Python Mac — the common case — silently lost the deep adapter and fell back to the guided path, which reads nothing. Combined with RISK-MAC-SOCKET-CREATION this meant no Mac could ever run a Verified diagnosis. | `macos_profile_params()` resolves the framework launcher before enumerating it, and `test_g1_every_exec_literal_has_its_canonical_form_enumerated` fails if any supplied literal resolves to a path no literal enumerates. | Dave | Closed — fixed |
| RISK-WOW-GATE-SCORES-SHAPE | The Wow Gate scores whether a ledger has the right shape, not whether its claims are supported, so a fabricated diagnosis outscores an honest one. | Independent review, 2026-09-03, reproduced at `45ceca5`. A ledger with all fourteen expectations `UNKNOWN` and evidence-free, zero observations, and every insight citing a fabricated evidence id scored **95/100, passed, zero hard failures** through the shipped `scripts/run_wow_gate.py`. An honest run with three real recommendations and genuinely assessed families scored **84 and failed**. Causes: `wow_gate.py:58-76` scores `UNKNOWN` identically to the five determinate states; `:49-55` tests a condition `WorkflowEdge.evidence_ids = Field(min_length=2)` already guarantees; `:101-114` never reduces for absent evidence; `:79-89` awards 8 points for zero recommendations. Separately `manual-proposal` (`:131`) is unreachable because `WorkReceipt.submission_route` is `Literal["engine-work-packet"]` (`work.py:318`), and three of the plan's nine hard-failure classes are unimplemented. | The gate is the programme's only measure of whether the autonomous diagnosis works. While it scores shape, no run's number is evidence, and the acceptance bar of "90 or better" cannot be met meaningfully. | Every listed cause is fixed red-first or withdrawn in writing. Scoring (`ba1a7cc`): `UNKNOWN` earns nothing, a determinate state scores only with evidence the ledger holds, workflow quality counts distinct held corroborations rather than edges, evidence integrity is the supported fraction of all claims, and zero recommendations score zero. Audit binding (`7d8e929`): the grader grades the audit the ledger was closed with and refuses any other; the unreachable `manual-proposal` and `digest-drift` classes are withdrawn in writing in favour of that binding, and `dishonest-operational-state` is withdrawn in writing (`ba1a7cc`, recorded in `_evidence_integrity`) until a definition distinguishes it from the renderer's "working well". Of the plan's nine classes, `incomplete-packets`, `missing-expectation` (an absent manifest hard-fails; a family-free catalogue yields fourteen loud not-gated rows, `a20924c`), `unsupported-claim`, `missing-rich-surprise` and `private-canary` (`98a711f` guards what the CLI prints) fire in tests; `too-many-recommendations` remains a fail-closed backstop that a model-validated ledger cannot reach because the ledger itself refuses an over-cap list before grading. The two-pass regrade (design item 9, this commit) grades pass-1 completeness separately from pass-2 depth: `WowGrade.pass_one_completeness` counts rows determinate-with-held-evidence or loudly priced, an unpriced Unknown or tampered not-gated placeholder is the `unpriced-unknown` hard failure, a FOCUSED ledger whose selected families hold any silent not-assessed member is the `sampled-selection` hard failure (the gate-level mirror of the engine's close rule at `6051784`), focused depth is scored on the selected families, and the shipped `run_wow_gate.py` emits the sub-score. The reproduction is pinned in `tests/evals/test_wow_gate.py`: the all-Unknown evidence-free ledger hard-fails and scores below 90, an honestly evidenced ledger passes, and a shape-perfect evidence-free focused ledger hard-fails while the honest focused run outscores it. | Dave | Closed — fixed |
| RISK-GUIDED-COMPARE-TRUSTS-ARTIFACT | Guided comparison reads back the stored `reconciled-proposals` artifact and only shape-validates it, instead of re-deriving it from the receipts it just reconciled. | Independent review, 2026-09-03, reproduced at `45ceca5`. Replacing the artifact on a run at `ANALYSIS_COMPLETED` with 20 fabricated `worth-borrowing` recommendations citing an evidence token never minted for the run was accepted into the ledger — against a cap of 10. `orchestrator.py:797-805` uses `ValidatedProposal.model_validate`, which checks none of what `validate_proposal` checks. The identical `_put` manipulation one artifact earlier is already covered by `test_guided_work_fails_closed_for_tampered_stored_diagnosis_input` and correctly fails closed. | Every upstream tamper-evidence guard the engine builds — re-derived queue, recomputed response digests, packet binding — is bypassable at the one artifact that actually reaches the ledger. The product's central claim is that conclusions are evidence-bound. | The guided branch of `_compare_ledger` now re-derives the final proposal set from the validated response records via `_final_reconciled_proposals` — the same derivation `_complete_analysis` performs, extracted into one shared helper so the two paths cannot drift — and refuses with a typed `DiagnosisStateError` when the stored `reconciled-proposals` artifact differs from the re-derived set in any way, added or dropped, before any ledger artifact is written. Only the re-derived set reaches the comparer; the stored artifact remains an audit record. The register's reproduction (20 fabricated `worth-borrowing` recommendations citing an unminted token, and a strict-subset truncation) both fail closed in `tests/diagnosis/test_orchestrator.py`, each observed accepted to a closed ledger on the unfixed tree; an honest untampered run still closes with the identical ledger. | Dave | Closed — fixed |
| RISK-GUIDED-RUN-WEDGE | Aggregate proposal limits are enforced only after every specialist receipt is already final, so an ordinary run can reach a terminal state with no exit. | Independent review, 2026-09-03, reproduced at `45ceca5`. Eight normal specialists each citing two distinct engine-minted evidence tokens for the same candidate — each response individually accepted — exceeds `MAX_EVIDENCE_IDS = 8` (`orchestrator.py:1218`) when the sceptical packet's context is built. `submit_work` then raises before the retry is recorded, `advance` refuses as incomplete, and `work` returns the same packet indefinitely. The `MAX_RECOMMENDATIONS = 10` variant (`:1238`) wedges identically. | This is normal specialist behaviour, not an attack: citing two pieces of evidence is what a good specialist does. A wedged run cannot be completed or abandoned cleanly, and the failure surfaces as a raw pydantic error rather than a typed refusal. | Evidence unions are truncated, over-cap recommendations are refused at submission. When agreeing proposals for one candidate together cite more than `MAX_EVIDENCE_IDS` distinct tokens, the reconciled result cites exactly the first 8 of the sorted union — deterministic and order-independent; evidence breadth is corroboration, so no conclusion is lost. Applied wherever evidence is unioned across agreeing proposals: `_normal_reconciled_proposals` (orchestrator) and `_coalesce_group` (specialists). The `MAX_RECOMMENDATIONS` cap is enforced at `submit_work`: a normal response that would push the distinct recommendation-candidate count over 10 is treated as a malformed specialist response — it burns the bounded attempt, is recorded as an empty rejection, and raises the typed retry message; an exact replay of an accepted response stays idempotent because the prospective count excludes the packet's own receipts. The reconciliation-time cap check remains as a fail-closed backstop against tampered stores; an honest run can no longer reach it. Both wedges reproduced through the real engine and observed to close after the fix. | Dave | Closed — fixed |
| RISK-COLD-PASS-2026-09-04 | A cold-context adversarial review of the 2026-09-03/04 work found six exploitable defects the authors' own red-first tests had missed: a forgeable in-band disagreement sentinel that turned an honest Unknown into a scored conclusion; the WO-022 footer fallback leaking the account name (symlinked home / XDG outside home) while /var/home layouts made Lens refuse its own report; a ten-factor-tuple candidate wedging a guided run with no exit; the run store losing finalized receipts under the newly shipped parallel round; the path guard refusing ordinary vaults containing folders named private/home/Users while missing /root, /srv, /opt, /Volumes and /var/home; and a strip-the-digest-line bypass of the saved-ledger tamper binding that a test explicitly certified as intended. | Cold adversarial pass, 2026-09-04, against `c3deb3b`; every finding reproduced with invented fixtures through real shipped surfaces; two of the holes were certified by same-day tests — the exact failure pattern the completion goal names. | Score integrity (a specialist could launder a conclusion), privacy (account name in the most shareable artifact), and availability (two fresh no-exit wedges) — on the very build meant to produce the first trustworthy number. | Fixed same day, each red-first: disagreement is now a structural engine-set fact and sentinel-shaped reasons are refused at the wire (`d58194f`); the footer never renders an absolute path and the guard matches absolute-path shape at token boundaries with the widened vocabulary (path-guard fix commit); factor tuples are capped at submit time where the bounded retry can reject, the run store serialises mutations per run and refuses stale saves (`9f346c7`); a supplied ledger now requires the report's recorded digest and the MCP server gained the CLI's redacted crash boundary (`a8f22c8`). The cold pass also returned a clean bill on the dispute-adjudication, recommendation-cap, artifact re-derivation, sceptical-lock, and family-contract attack surfaces. | Dave | Closed — fixed |
| RISK-EXTERNAL-PASS-2026-09-07 | The first fully external adversarial review (a different vendor's model, working from the committed brief) found six defects: an unsigned stored boolean (`family_contract_present`) trusted on reload, letting a disk-level tamper enable release-distance conclusions a signed-familyless catalogue must refuse; a punctuation boundary gap letting `word→/Users/...`-shaped absolute paths through the payload guard into closed reports; typed-model refusals echoing attacker-chosen extra-field names from tampered stored artifacts through CLI stderr and the MCP wire; legend labels/relative paths propagating to ledger and saved report with the containment test never checking those surfaces; the superseded-question allowance validating stored packets while queue re-derivation still used current wording, wedging genuine old-question runs with no exit; and the founder-resolution mechanism stamping materially changed family content with the historical approval. | External review, 2026-09-07, against `f9d25b3`; all six findings re-verified in-repo red-first with invented fixtures through real shipped surfaces before fixing; the review also reported the signature, replay, sceptical-lock, overflow, crash-redaction and footer surfaces held. | Score integrity (stored-flag authority laundering), privacy (two output-leak channels), availability (an upgrade-day wedge on every in-flight run), and governance (approval not bound to approved content). | Fixed same day, each red-first: the catalogue slice is re-derived from the signature-verified envelope on every reload and a disagreeing stored slice is refused (`46e1632`); the guard boundary is inverted so only word/path characters continue a path, with the deliberate relative-path and named-folder allowances regression-pinned (`25d093d`); refusals name only declared model fields and collapse unknown keys, with CLI/MCP refusal text routed through the payload guard (`46e1632`); the report privacy contract is stated and pinned — local reports may quote labels and vault-relative paths, outbound surfaces (share card, grade JSON, refusals, crash logs) proven clean by positive-controlled tests (`25d093d`); queue re-derivation reconstructs each packet under its issued wording via a versioned question contract, and the wedged-run-to-CLOSED path is a test (`9b4cd52`); the founder resolution is digest-bound per family and changed content reopens review with fresh TODOs (`cdc708a`). | Dave | Closed — fixed |
| RISK-NON-LINEAGE-COMPONENT-MATCH-2026-09-07 | On a system where Dex is not installed, a capability name the person happens to share with the signed catalogue is still admitted as a matched family component. | Introduced by the presence fix (`c2c21ac`, AGENTS.md F8). While "Dex is not installed" meant "no observation matched any signed name", the two states were mutually exclusive by construction and `ComparisonLedger` enforced that they could never coexist. Deciding presence from Dex's own release record makes the combination reachable and ordinary: 85 of the catalogue's 117 capability ids carry no namespace prefix, so a person who never installed Dex can own a `journal` or a `daily-plan` of their own. The invariant was dropped so their ledger stays buildable. | Latent, not live. A match on such a run reaches no reader today: the run renders on the job axis, every family row carries the fixed non-lineage reason, and the release-gap block states plainly that Dex is not installed. The risk is that a later consumer of `family_entries` reads a coincidence as coverage — the same class of error as F7 and F8, one layer down. | Suppressing the match at source (admitting no component match when Dex is absent, since nothing local can be evidence of a component of software that is not there) was implemented and reverted the same session: it failed 37 tests across seven files, because the fixtures asserting family overlap represent Dex installs and convey it only through name overlap — the inversion itself, encoded as fixtures (AGENTS.md F6). That migration needs reviewing rather than rushing. Until then the reasoning is recorded at the dropped invariant in `comparison.py` and the report keeps the job-axis voice on these runs. | Dave | **Open — deferred, needs a reviewed fixture migration** |
| RISK-FIRST-READ-REPORT-VOICE-2026-09-08 | The first person-mimicking run against a realistic stale vault closed correctly and produced a report a person cannot comfortably read: the reverse-direction section claimed personal authorship ("these are yours alone") over a list that includes Dex's own retired stock, rendered as one identical boilerplate row per item for a hundred-plus items; the one grounded recommendation rendered three times verbatim (ranked move, worth-borrowing, workflow connection); the coverage block spoke in the confession voice ("not examined... nothing here says present or absent") on a run that had assessed every signed area, because the catalogue's one manual-only family row defeats the coverage-first story by carrying the same `not-assessed` disposition as a genuine skip; the installed CLI welcome lists the two new sharing commands twice; and `dex-lens diagnosis --help` never names the mandatory `intake` step. | Mimic-user run on merged main, 2026-09-08, against the founder's vault clone (aggregates only; the report itself stays on this machine). Each defect re-verified red-first with invented fixtures in `tests/diagnosis/test_report_readability.py` and the two CLI copy tests. | The product's first impression: a correct run that reads as slop undermines the trust every honest refusal earns, and the authorship overclaim is F7 rendered to the person it most misleads. | Fixed in this change, each red-first: unique-to-you claims possession not authorship, adds the older-Dex caveat whenever the run knows the install is behind, and renders grouped by capability type instead of a row per item (founder decision 2026-09-08, annotated on AGENTS.md F7); a recommendation proposal no longer doubles as a workflow connection, and the worth-borrowing section points to the ranked list instead of repeating it; a manual-only family row no longer flips the coverage story into the confession — it is named with its signed reason instead; the welcome names each command once; the diagnosis help names intake. | Dave | Closed — fixed |

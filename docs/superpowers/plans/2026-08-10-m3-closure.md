# M3 Source Alpha Closure Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close every repository-controlled M3 proof gap while failing closed on host capabilities that the exact CI runner cannot prove.

**Architecture:** Preserve the read-only concierge and adapter boundary. Expand typed inventory coverage to browser/session state, add real child-process cancellation and stage-by-stage byte-identity proofs, and run the bind-mount hostile fixture in a disposable privileged Linux container. On macOS, disable deep inspection when socket-creation denial cannot be proven and use the existing guided path.

**Tech Stack:** Python 3.11/3.12, Pydantic, pytest, GitHub Actions, Docker, Linux namespaces, macOS Seatbelt.

---

### Task 1: Fail-closed macOS deep inspection

**Files:**
- Modify: `src/capability_exchange/adapters/claude_code/containment.py`
- Modify: `tests/adapters/claude_code/test_containment.py`
- Modify: `tests/egress/test_g2_macos_sandbox_profile.py`

- [ ] Add a failing test proving `default_strategy()` refuses the macOS deep adapter when the runtime proof cannot deny socket creation, while retaining the guided fallback message.
- [ ] Run `.venv/bin/python -m pytest -q tests/adapters/claude_code/test_containment.py tests/egress/test_g2_macos_sandbox_profile.py` and confirm the macOS-selection assertion fails.
- [ ] Make `MacOSStrategy.availability()` fail closed unless every G1 runtime proof, including socket creation denial, is enforceable; do not weaken the Linux strategy or simulated profile tests.
- [ ] Re-run the focused tests and confirm green.

### Task 2: Inventory browser and session fields

**Files:**
- Modify: `src/capability_exchange/boundary/data_inventory.yaml`
- Modify: `src/capability_exchange/concierge/security.py`
- Modify: `src/capability_exchange/concierge/journey.py`
- Modify: `scripts/check_inventory.py`
- Test: `tests/boundary/test_check_inventory.py`
- Test: `tests/concierge/test_r3_security.py`

- [ ] Add failing inventory tests for the session token digest, CSRF digest, cookie metadata, approved-scope references, session expiry, and journey-state fields.
- [ ] Run `.venv/bin/python -m pytest -q tests/boundary/test_check_inventory.py tests/concierge/test_r3_security.py` and confirm missing inventory coverage is reported.
- [ ] Represent persistable/session fields through `InventoriedModel`-compatible records or explicit inventory declarations, marking secrets as memory-only, never shared, and deletion-on-session-close.
- [ ] Extend the inventory checker so a new concierge session field without a declaration fails CI.
- [ ] Re-run focused tests and `python scripts/check_inventory.py` until green.

### Task 3: Prove cancellation and every-stage exit

**Files:**
- Modify: `tests/concierge/test_r3_collection.py`
- Modify: `tests/concierge/test_fallback_e2e.py`
- Modify: `tests/concierge/test_journey.py`
- Modify: `tests/concierge/test_local_server.py`

- [ ] Add a failing integration test using the real contained child, a read barrier, and cancellation; assert child termination occurs inside the declared bound and partial result files do not exist.
- [ ] Add parameterized failing tests for decline/exit at each stage 1–6, comparing a complete byte manifest of the inspected tree before and after.
- [ ] Run the focused tests and confirm the new assertions fail for missing integration support.
- [ ] Add only the controller/test seams required to observe child termination and session cleanup; preserve the security lock and collection boundary.
- [ ] Re-run the focused tests and the complete concierge suite.

### Task 4: Run the privileged bind-mount proof in CI

**Files:**
- Create: `scripts/Dockerfile.g1-bind-mount`
- Create: `scripts/g1_bind_mount_gate.py`
- Modify: `.github/workflows/ci.yml`
- Test: `tests/fixtures/hostile/test_g1_bind_mount_escape.py`

- [ ] Add a failing gate-script test that rejects a skipped bind-mount fixture, a missing child report, or any non-passing containment assertion.
- [ ] Run the test locally and confirm it fails because the current host cannot create the namespace.
- [ ] Build a disposable Linux image and CI job that runs only the bind-mount hostile module with the narrow `SYS_ADMIN` capability, no network, read-only root, bounded tmpfs, dropped remaining capabilities, and uploads the JSON evidence.
- [ ] Make the gate explicitly fail on any skip or warning that says the fixture was unproven.
- [ ] Validate workflow syntax and run all locally runnable gate-script tests.

### Task 5: Correct the source-of-truth documents

**Files:**
- Modify: `docs/handoff/HANDOFF.md`
- Modify: `docs/STATUS.md`
- Modify: `docs/handoff/M1-ACCEPTANCE.md`
- Modify: `docs/RISK-REGISTER.md`
- Modify: `README.md`

- [ ] Add a documentation assertion test or grep check for the recorded D0 hash and merged PR #4 state.
- [ ] Replace stale “pending/no code/draft PR” statements with the recorded D0 decision and merged-main evidence.
- [ ] Record the fail-closed macOS fallback, bind-mount runner evidence location, and remaining external ownership/signoff dependencies without claiming a real pilot.
- [ ] Run the full test suite, lint, inventory check, and wheel test.

